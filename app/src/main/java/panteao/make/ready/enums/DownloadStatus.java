package panteao.make.ready.enums;

public enum DownloadStatus {
    START,
    REQUESTED,
    PAUSE,
    DOWNLOADING,
    SERIES_DOWNLOADING,
    started,
    DOWNLOADED,
    FAILED,
    Completed,
    PAUSED
}
